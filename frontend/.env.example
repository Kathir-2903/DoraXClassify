# Leave empty to use the Vite dev proxy (/api → VITE_PROXY_TARGET).
VITE_API_URL=
VITE_PROXY_TARGET=http://localhost:8000
# Show the demo login shortcuts on the sign-in page.
VITE_SHOW_DEMO_LOGINS=true
